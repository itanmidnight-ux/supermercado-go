# Fail2Ban filter for Supermercados Go authentication attempts

[Definition]

failregex = ^.*\[AUTH\] Login fallido.*ip=<HOST>.*$
            ^.*\[AUTH\] Token invalido.*ip=<HOST>.*$
            ^.*Rate limit exceeded.*ip=<HOST>.*$

ignoreregex =
