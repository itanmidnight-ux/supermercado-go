name: pedidos_app
description: Sistema de gestión de pedidos WhatsApp - Concentrados Monserrath
publish_to: 'none'
version: 2.0.0+3

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  http: ^1.2.1
  google_fonts: ^6.2.1
  provider: ^6.1.2
  shared_preferences: ^2.5.5
  flutter_secure_storage: ^9.2.2
  flutter_slidable: ^3.1.1
  intl: ^0.19.0
  url_launcher: ^6.3.0
  onesignal_flutter: ^5.2.5
  audioplayers: ^6.7.0
  image_picker: ^1.1.2
  record: ^7.0.0
  path_provider: ^2.1.3
  cached_network_image: ^3.3.1
  video_player: ^2.8.3
  file_picker: ^8.0.0+1
  smooth_page_indicator: ^1.2.0+3
  fl_chart: ^0.69.0
  sqflite: ^2.3.3+2
  path: ^1.9.1
  connectivity_plus: ^6.0.3
  flutter_local_notifications: ^17.2.3
  geolocator: ^13.0.1
  device_info_plus: ^10.1.2
  flutter_map: ^7.0.2
  latlong2: ^0.9.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.14.1

flutter:
  uses-material-design: true

# file_picker 8.x quedó anclado a flutter_plugin_android_lifecycle en su
# version mas reciente (2.0.35), que exige compileSdk>=36 para todo lo que
# depende de ella -- pero el AAR de file_picker sigue compilado contra 34,
# asi que rompe el build. Se fija una version anterior que no exige ese
# piso; file_picker 11.x lo arregla pero rompe la compilacion Kotlin nativa
# bajo AGP 9 en este entorno.
dependency_overrides:
  flutter_plugin_android_lifecycle: 2.0.22

flutter_launcher_icons:
  android: true
  ios: false
  image_path: "assets/icon/app_icon.png"
  min_sdk_android: 23
  adaptive_icon_background: "#1A3009"
  adaptive_icon_foreground: "assets/icon/app_icon_foreground.png"
